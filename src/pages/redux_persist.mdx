import WebPlayer from '../components/WebPlayer'
import example from '../examples/redux_persist'

Redux Persist是一种极其简单的方法，它可以将整个redux store持久化到磁盘上，并在应用程序再次启动时将其加载到内存中。Redux Persist是<code>AsyncStorage</code>之上的一个抽象层。你可以在[docs](https://github.com/rt2zz/redux-persist)中阅读更多关于Redux Persist的内容。

<WebPlayer {...example} />
