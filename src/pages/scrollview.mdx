import WebPlayer from '../components/WebPlayer'
import example from '../examples/scrollview'

`ScrollView`用在可滚动的内容。
`ScrollView`s are used for scrollable content. 

它们比较适合滚动比较少（小于30）的内容，支持水平和垂直滚动。对于items比较多的情况，考虑使用`FlatList`获得更好的性能。

<WebPlayer {...example} />
