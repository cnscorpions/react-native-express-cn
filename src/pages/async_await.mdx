import EditorConsole from '../components/EditorConsole'
import example from '../examples/async_await'

我们能在一个函数的函数名之前加上`async`关键字，把函数返回值包裹到一个`Promise`中。我们能在`async`函数中使用`await`关键字，停止执行代码，直到promise被resolved或者rejected。

可以使用`try/catch`来捕获异常，就像在同步代码中使用一样。

<EditorConsole {...example} />
