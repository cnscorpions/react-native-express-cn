import EditorConsole from '../components/EditorConsole'
import example from '../examples/static_class_properties'

正如我们在ES2015章节看到的，class的静态方法作为ES2015标准的一部分存在。在ES2016中，我们也能使用`static`关键字来声明静态属性。静态属性只存在在class上。

<EditorConsole {...example} />
