import EditorConsole from '../components/EditorConsole'
import example from '../examples/dynamic_object_keys'

在ES5中，对象字面量keys总是被解释为一个字符串。ES6允许我们使用计算值作为对象字面量的keys，使用方括号语法：`[myKey]`。

<EditorConsole {...example} />
